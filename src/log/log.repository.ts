import { db } from "../utils/db.server";

const createLog = async (
  date: string,
  image: string,
  latitude: number,
  longitude: number
) => {
  // const ret = await db.startLog
}

export { createLog };